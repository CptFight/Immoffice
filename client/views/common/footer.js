Template.footer.rendered = function () {
	/*Session.set('force_logout',false);
	$( document ).idleTimer( 100000 );

    $( document ).on( "idle.idleTimer", function(event, elem, obj){
       
        toastr.warning(translate('warning_you_will_be_disconnected_if_you_dont_move_the_mouse'),translate('warning'));
       
        Session.set('force_logout',true);
        setTimeout(function() {
        	if(Session.get('force_logout')){
        		Meteor.logout(function(err) {
	          
	        	});
        	}
	       
	    }, 6000);


    });

    $( document ).on( "active.idleTimer", function(event, elem, obj, triggerevent){
        // function you want to fire when the user becomes active again
        toastr.clear();
     
        Session.set('force_logout',false);
        toastr.success(translate('welcome_back'),translate('you_are_back'));

    }); 
*/
};
/*

Template.footer.destroyed = function(){
    // Destroy idle timer
    //$( document ).idleTimer('destroy');
};

*/